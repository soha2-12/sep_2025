class Solution:
    def maximumHappinessSum(self, happiness, k):
        happiness.sort(reverse=True)
        total = 0
        for i in range(k):
            gain = happiness[i] - i
            if gain > 0:
                total += gain
        return total
